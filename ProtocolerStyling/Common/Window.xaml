<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ProtocolerStyling.Common">
    <!--#region Common-->
    <Style x:Key="BaseWindowStyle" TargetType="Window">
        <Setter Property="BorderThickness" Value="0" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource Mode=Self}}" Value="Maximized">
                <Setter Property="BorderThickness" Value="7" />
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <!--#endregion-->
    <!--#region Dark-->
    <Style  BasedOn="{StaticResource BaseWindowStyle}" x:Key="WindowStyleDark" TargetType="Window">
        <Setter Property="Background" Value="Black"/>
    </Style>
    <!--#endregion-->
    <!--#region Light-->
    <Style BasedOn="{StaticResource BaseWindowStyle}" x:Key="WindowStyleLight" TargetType="Window">
        <Setter Property="Background" Value="White"/>
    </Style>
    <!--#endregion-->
</ResourceDictionary>